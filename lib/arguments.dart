
class GroupPageArguments {
  final String groupName;
  final String groupTagline;
  final String groupImageURL;
  final int groupSize;
  final bool favoriteGroup;

  GroupPageArguments(this.groupName, this.groupTagline, this.groupImageURL,
      this.groupSize, this.favoriteGroup);
}
